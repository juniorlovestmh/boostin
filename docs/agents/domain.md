# Domain Docs

Boostin uses a single root `CONTEXT.md` and system-wide decisions under
`docs/adr/`. Read the context and relevant ADRs before changing domain language,
privacy boundaries, import behavior, or report semantics. Surface conflicts
instead of silently overriding an ADR.
